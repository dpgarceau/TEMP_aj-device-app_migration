# AeroJudge config version, updated on 30-June-2026
#
# Used only in official AeroJudge devices with:
# -- (1) Device Serial numbers DPG-105, and,
# -- (2) PCB Revision 3.6x 
#
# NOT TO BE USED ON OLDER LEGACY IMAC-ORG DERIVED SYSTEMS
# Save as /boot/config.txt


# ==================================================== #
# General Pi Config Settings


# For more options and information see
# http://rpf.io/configtxt
# Some settings may impact device functionality. See link above for details

# uncomment if you get no picture on HDMI for a default "safe" mode
#hdmi_safe=1

# uncomment the following to adjust overscan. Use positive numbers if console
# goes off screen, and negative if there is too much border
#overscan_left=16
#overscan_right=16
#overscan_top=16
#overscan_bottom=16

# uncomment to force a console size. By default it will be display's size minus
# overscan.
#framebuffer_width=1280
#framebuffer_height=720

# uncomment if hdmi display is not detected and composite is being output
#hdmi_force_hotplug=1

# uncomment to force a specific HDMI mode (this will force VGA)
#hdmi_group=1
#hdmi_mode=1

# uncomment to force a HDMI mode rather than DVI. This can make audio work in
# DMT (computer monitor) modes
#hdmi_drive=2

# uncomment to increase signal to HDMI, if you have interference, blanking, or
# no display
#config_hdmi_boost=4

# uncomment for composite PAL
#sdtv_mode=2

# Uncomment some or all of these to enable the optional hardware interfaces
dtparam=i2c_arm=on
#dtparam=i2s=on
#dtparam=spi=on


# Additional overlays and parameters are documented /boot/overlays/README

# Legacy PWM Config to delete after setup is validated.
# Enable hardware PWM audio for AeroJudge headphone amp
# Pi 4 requires audremap overlay for GPIO12/GPIO13 audio routing
#dtparam=audio=on
#audio_pwm_mode=2
#dtoverlay=audremap,pins_12_13

# Audio updated to hifiberry-dacplus for AeroJudge headphone amp
# Hold the active-high headphone amp disabled until the audio service is ready
gpio=8=op,dl
dtparam=audio=off
dtoverlay=hifiberry-dacplus




# Automatically load overlays for detected DSI displays
display_auto_detect=1

# Enable DRM VC4 V3D driver
dtoverlay=vc4-kms-v3d
max_framebuffers=2

# Disable compensation for displays with overscan
disable_overscan=1

# ==================================================== #
# Model-Specific Settings

[pi4]
# Run as fast as firmware / board allows
arm_boost=1

[all]
# Disabling Bluetooth (required - frees UART for GPIO 14/15 button use)
dtoverlay=disable-bt



# ==================================================== #
# GPIO KEY MAPS

# Device GPIO Button Mapping 
dtoverlay=gpio-key,gpio=6,active_low=1,gpio_pull=up,keycode=77   # J3 NEXT (KP6)
dtoverlay=gpio-key,gpio=12,active_low=1,gpio_pull=up,keycode=81  # J4 -1.0 (KP3)
dtoverlay=gpio-key,gpio=13,active_low=1,gpio_pull=up,keycode=80  # J5 -0.5 (KP2)
dtoverlay=gpio-key,gpio=16,active_low=1,gpio_pull=up,keycode=76  # J6 ZERO (KP5)
dtoverlay=gpio-key,gpio=26,active_low=1,gpio_pull=up,keycode=71  # J7 BREAK (KP7)
# PCB 3.6 hardware conflict: GPIO20 is claimed by the kernel I2S pin group.
# DEADLINE remains unavailable until PCB 3.61 reroutes it away from GPIO18-21.
#dtoverlay=gpio-key,gpio=20,active_low=1,gpio_pull=up,keycode=83  # J8 DEADLINE (KPDOT)

#dtoverlay=gpio-key,gpio=4,active_low=1,gpio_pull=up,keycode=31  # J9 SPARE (KP S- unused)
dtoverlay=gpio-key,gpio=14,active_low=1,gpio_pull=up,keycode=79  # J10 NOT OBSERVED (KP1)
dtoverlay=gpio-key,gpio=15,active_low=1,gpio_pull=up,keycode=82  # J11 CALLER (KP0)
dtoverlay=gpio-key,gpio=17,active_low=1,gpio_pull=up,keycode=72  # J12 +0.5 (KP8)
dtoverlay=gpio-key,gpio=27,active_low=1,gpio_pull=up,keycode=73  # J13 +1.0 (KP9)
dtoverlay=gpio-key,gpio=22,active_low=1,gpio_pull=up,keycode=75  # J14 BACK (KP4)

# Audio Channel Mapping
# Configured above in general settings - I2S uses GPIO 18, 19, and 21

# Audio Volume Thumbwheel Mapping
dtoverlay=gpio-key,gpio=23,active_low=1,gpio_pull=up,keycode=114  # Vol Down (terminal 2)
dtoverlay=gpio-key,gpio=9,active_low=1,gpio_pull=up,keycode=115   # Vol Up (terminal 4)
#dtoverlay=gpio-key,gpio=10,active_low=1,gpio_pull=up,keycode=113  # Mute Toggle (terminal 3)

# Shutdown and Power_Off Mapping
dtoverlay=gpio-shutdown,gpio_pin=25,active_low=1,gpio_pull=up,debounce=3000
dtoverlay=gpio-poweroff,gpiopin=24



# ==================================================== #
# END
